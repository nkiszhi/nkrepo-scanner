# 运行时生成物
signatures/*.db
signatures/*.db.bloom
signatures/*.db.bloom.legacy
signatures/*.db.bloom/
signatures/*.db-journal
signatures/*.db-shm
signatures/*.db-wal
signatures/*.db.migrated
signatures/*.db.migrated-shm
signatures/*.db.migrated-wal
signatures/*.db.shards/
signatures/*.db.shards.legacy/

# 病毒库原始文件与解包产物（README 有下载/重建方法）
cvd/
extracted/
*.cvd
signatures/hdb/
hdb/

# 上传样本目录（运行时生成，程序启动时自动创建，整个目录不入库）
# 注: 样本原始字节 (<sha256>) 与扫描报告 (<sha256>.json) 均存于 uploads/ 同目录;
#     eicar.com 为 EICAR 测试样本, 因安全工具会拦截其文件读写, 不入库;
#     本地测试可手动生成 (见 README「测试样本」)
uploads/

# 调试/迁移临时文件
_mig_*.py
_mig_*.txt
_tmp_*.py
_tmp_*.py.bak
_tmp_*.json
_reshard_*.log
_reshard_*.log.bak
server_stdout.log
*.log

# Python
__pycache__/
*.pyc
venv/

# 编辑器/系统
.vscode/
.idea/
Thumbs.db
.DS_Store
*.un~
*~

# WorkBuddy 项目记忆
.workbuddy/

# 第三方 YARA 规则仓库 (git clone 收集, 不入库; 见 README「YARA 规则库」)
yara_sources/

# 签名库分片临时目录
signatures/*.db.shards.resharding/
signatures/*.db.shards.resharding.done/

# 分片数调整产生的运行时备份目录
signatures/*.bak_4shard/
signatures/*.bak_*/

# 本地导出的哈希清单 / ssdeep C 扩展构建临时目录
signatures/md5_out.hdb
_ssdeep_build/
