# 运行时生成物
signatures/*.db
signatures/*.db.bloom
signatures/*.db.bloom.legacy
signatures/*.db.bloom/
signatures/*.db-journal
signatures/*.db-shm
signatures/*.db-wal
signatures/*.db.migrated
signatures/*.db.shards/
signatures/*.db.shards.legacy/

# 病毒库原始文件与解包产物（README 有下载/重建方法）
cvd/
extracted/
*.cvd

# 上传样本目录（运行时生成，程序启动时自动创建，整个目录不入库）
# 注: eicar.com 为 EICAR 测试样本, 因安全工具会拦截其文件读写, 不入库;
#     本地测试可手动生成 (见 README「测试样本」)
uploads/

# 调试/迁移临时文件
_mig_*.py
_mig_*.txt

# Python
__pycache__/
*.pyc
venv/

# 编辑器/系统
.vscode/
.idea/
Thumbs.db
.DS_Store
